enum ScreenStatus {
  details,
  authentication,
  success,
  error,
}

enum LogLevel {
  none,
  all,
}

enum ThreeDsState {
  threeDs,
  threeDs2,
  none,
}

enum CardPayButtonStatus {
  not_ready,
  ready,
  processing,
  success,
  fail,
}
